import Color from "../day14/color.js"

class OpticalObject {

    interceptDistance(ray) {
        return 0
    }

    handle(ray) {
        return new Color()
    }
    
}

export default OpticalObject