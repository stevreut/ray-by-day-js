class CodeExtractor {
    async getCodeLines(codeUrl,firstLine,lastLine) {
        console.log('getCode called with', codeUrl, firstLine, lastLine)
        return 'test code'
    }
}

export default CodeExtractor